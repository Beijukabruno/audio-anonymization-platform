# Environment Configuration for Audio Anonymization Platform

# Database Configuration (Production)
POSTGRES_DB=audio_anony
POSTGRES_USER=audio_user
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD_IN_PRODUCTION

# Database URL (constructed from above or override entirely)
# DATABASE_URL=postgresql://audio_user:password@postgres:5432/audio_anony

# Gradio Server Configuration
GRADIO_SERVER_NAME=0.0.0.0
GRADIO_SERVER_PORT=7860

# Application Settings
# LOG_LEVEL=INFO
